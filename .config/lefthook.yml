pre-commit:
  jobs:
    - name: csharpier format check
      run: just lint-check

    # - name: oxlint check
    #   run: just ts-lint

commit-msg:
  jobs:
    - name: conventional commits
      run: bunx commitlint --edit {1}
